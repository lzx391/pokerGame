# Vue CLI 在 npm run dev 时会加载本文件；若与根目录 .env 存在同名变量，以本文件为准（后加载覆盖）。
# 因此：日常不要在下面写 VUE_APP_*，把多实例、大厅、map 等写在 .env（由 .env.example 复制），避免「草稿里两行配置盖住 .env 里的 /c-api」。
# 同域开发入口：http://127.0.0.1:8880（见 docker/nginx/README-dp-dev-two-jvm.md）
