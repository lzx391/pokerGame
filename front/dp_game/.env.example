# 复制为 front/dp_game/.env 后按需改；重启 npm run dev 生效（勿把密钥提交进 Git）。
# 说明：docker/nginx/README-dp-dev-two-jvm.md
# .env.development 仓库里仅保留注释：真正配置写在 .env，避免「开发模式专用文件」覆盖 .env。

# 可选：大厅 publicRooms 随机打到的前缀；不配则从 VUE_APP_DP_DEV_WS_HTTP_MAP 自动收集（含 /c-api）。
# VUE_APP_LOBBY_API_BASES=/dev-api,/b-api,/c-api
VUE_APP_DEV_JAVA_PORT=8088
# wsRoute → axios 前缀：逗号分隔 key:value；path 片段与纯数字端口可混写；B 与 C 写在同一行。
# 不配本变量时，代码内默认含 B+C（与 8089 对称）：dp-ws-b、8089→/b-api，dp-ws-c、8090→/c-api。只跑两台时请改为仅：dp-ws-b:/b-api,8089:/b-api
# 三 JVM + Nginx 已配 /c-api、8090 时示例（双 JVM 请删掉 C 段或整行）：
VUE_APP_DP_DEV_WS_HTTP_MAP=dp-ws-b:/b-api,dp-ws-c:/c-api,8089:/b-api,8090:/c-api
