# 云服务器 docker compose 用：复制为 .env 后填写方舟大模型五项（勿提交 .env）
#   copy .env.example .env
#
# 其余（MySQL/Redis 口令、镜像 tag、端口等）已写在 docker-compose.yml 默认值中。

ARK_API_KEY=
ARK_ENDPOINT_ID=
ARK_BASE_URL=
ARK_REASONING_EFFORT=
ARK_THINKING_TYPE=
