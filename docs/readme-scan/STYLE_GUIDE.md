# README 结构约定（从根 README.md 提取）

- 中英**同一文件**：上半中文、下半 `## English` 镜像。
- 文首声明：与代码不一致时以仓库实现为准。
- 章节顺序：给代码评审 → 怎么跑 → Mermaid → 项目一览 → 技术栈 → 环境 → 快速开始 → 测试 → 结构 → 文档索引 → 许可证。
- 技术名词保留英文；**不用 emoji**；相对路径链到 `docs/`。
- `TODO.md` 语气不直接粘贴进 README。
